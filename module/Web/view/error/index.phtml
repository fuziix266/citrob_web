<?php
/** @var \Laminas\View\Renderer\PhpRenderer $this */
$this->headTitle('Error del servidor');
$exception = $this->exception ?? null;
?>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Error | CITROB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <style>
        body { font-family: 'Lexend', sans-serif; background: #f8f7f5; display: flex; align-items: center; justify-content: center; min-height: 100vh; }
        .error-code { font-size: 8rem; font-weight: 900; color: #dc3545; line-height: 1; }
        pre { background: #1e293b; color: #e2e8f0; padding: 1.5rem; border-radius: 8px; font-size: 12px; text-align: left; overflow-x: auto; max-height: 400px; }
    </style>
</head>
<body>
    <div class="container text-center py-5">
        <div class="error-code">500</div>
        <h2 class="fw-bold mt-3">Error del servidor</h2>
        <p class="text-muted">Ocurrió un error inesperado. Por favor intenta más tarde.</p>
        <?php if ($exception): ?>
        <div class="mt-4 text-start">
            <p class="text-danger fw-bold"><?= htmlspecialchars($exception->getMessage()) ?></p>
            <pre><?= htmlspecialchars($exception->getTraceAsString()) ?></pre>
        </div>
        <?php endif; ?>
        <a href="/" class="btn btn-warning fw-bold mt-3">Volver al inicio</a>
    </div>
</body>
</html>
