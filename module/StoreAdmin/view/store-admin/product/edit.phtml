<?php $this->layout('store-admin/layout/layout'); ?>
<?php $p=$this->product; $cats=$this->categories; $editing=!empty($p); ?>
<?php $this->activePage='products'; $this->pageTitle=$editing?'Editar Producto':'Nuevo Producto'; ?>

<div class="bg-white rounded-3 border p-4" style="max-width:800px;">
  <form method="POST" action="/admin/products/<?= $editing?'edit/'.$p['id']:'edit' ?>" enctype="multipart/form-data">
    <div class="row g-3">
      <div class="col-md-8">
        <label class="form-label fw-600" style="font-size:13px;">Nombre del Producto *</label>
        <input type="text" name="name" class="form-control" value="<?= htmlspecialchars($p['name']??'',ENT_QUOTES) ?>" required>
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Categoría *</label>
        <select name="category_id" class="form-select" required>
          <?php foreach ($cats as $c): ?>
          <option value="<?= $c['id'] ?>" <?= ($p['category_id']??0)==$c['id']?'selected':'' ?>><?= htmlspecialchars($c['name'],ENT_QUOTES) ?></option>
          <?php endforeach; ?>
        </select>
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Precio (CLP) *</label>
        <input type="number" name="price" class="form-control" value="<?= $p['price']??'' ?>" required min="0">
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Stock</label>
        <input type="number" name="stock" class="form-control" value="<?= $p['stock']??0 ?>" min="0">
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Tipo</label>
        <input type="text" name="type" class="form-control" value="<?= htmlspecialchars($p['type']??'',ENT_QUOTES) ?>" placeholder="ej: Arduino">
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Badge</label>
        <input type="text" name="badge" class="form-control" value="<?= htmlspecialchars($p['badge']??'',ENT_QUOTES) ?>" placeholder="ej: Nuevo">
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Color Badge</label>
        <select name="badge_color" class="form-select">
          <?php foreach (['green','blue','orange','red','purple'] as $col): ?>
          <option value="<?= $col ?>" <?= ($p['badge_color']??'green')===$col?'selected':'' ?>><?= ucfirst($col) ?></option>
          <?php endforeach; ?>
        </select>
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Rating (0-5)</label>
        <input type="number" name="rating" class="form-control" value="<?= $p['rating']??4.5 ?>" min="0" max="5" step="0.1">
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Reseñas</label>
        <input type="number" name="reviews" class="form-control" value="<?= $p['reviews']??0 ?>" min="0">
      </div>

      <!-- Subir imagen -->
      <div class="col-12">
        <label class="form-label fw-600" style="font-size:13px;">Imagen del Producto</label>
        <div class="d-flex align-items-start gap-3">
          <!-- Preview -->
          <div id="imgPreviewBox" style="width:100px;height:100px;border-radius:12px;background:#f8fafc;border:2px dashed #e2e8f0;display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0;<?= !empty($p['image_url'])?'border-style:solid;border-color:#e2e8f0;':''; ?>">
            <?php if (!empty($p['image_url'])): ?>
            <img id="imgPreview" src="<?= htmlspecialchars($p['image_url'],ENT_QUOTES) ?>" alt="Preview" style="width:100%;height:100%;object-fit:contain;">
            <?php else: ?>
            <img id="imgPreview" src="" alt="Preview" style="width:100%;height:100%;object-fit:contain;display:none;">
            <span id="imgPlaceholder" class="material-icons" style="font-size:32px;color:#cbd5e1;">add_photo_alternate</span>
            <?php endif; ?>
          </div>
          <div class="flex-grow-1">
            <input type="file" name="image_file" id="imageFileInput" class="form-control" accept="image/jpeg,image/png,image/webp,image/gif" style="border-radius:10px;">
            <div class="text-muted mt-1" style="font-size:11px;">
              Formatos aceptados: JPG, PNG, WebP, GIF. Tamaño máximo recomendado: 2MB
            </div>
            <?php if (!empty($p['image_url'])): ?>
            <div class="mt-2 d-flex align-items-center gap-2">
              <span class="pill pill-success" style="font-size:11px;">
                <span class="material-icons" style="font-size:12px;vertical-align:-2px;">check_circle</span>
                Imagen actual guardada
              </span>
              <span class="text-muted" style="font-size:11px;"><?= basename($p['image_url']) ?></span>
            </div>
            <?php endif; ?>
          </div>
        </div>
      </div>

      <div class="col-12">
        <label class="form-label fw-600" style="font-size:13px;">Descripción</label>
        <textarea name="description" class="form-control" rows="3"><?= htmlspecialchars($p['description']??'',ENT_QUOTES) ?></textarea>
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Estado</label>
        <select name="active" class="form-select">
          <option value="1" <?= ($p['active']??1)?'selected':'' ?>>Activo</option>
          <option value="0" <?= !($p['active']??1)?'selected':'' ?>>Inactivo</option>
        </select>
      </div>
      <div class="col-md-4">
        <label class="form-label fw-600" style="font-size:13px;">Destacado</label>
        <select name="featured" class="form-select">
          <option value="0" <?= !($p['featured']??0)?'selected':'' ?>>No</option>
          <option value="1" <?= ($p['featured']??0)?'selected':'' ?>>Sí</option>
        </select>
      </div>
    </div>

    <div class="d-flex gap-3 mt-4">
      <button type="submit" class="btn btn-store rounded-pill px-5 fw-700">
        <span class="material-icons" style="font-size:16px;vertical-align:-3px;">save</span>
        <?= $editing?'Guardar Cambios':'Crear Producto' ?>
      </button>
      <a href="/admin/products" class="btn btn-outline-secondary rounded-pill px-4">Cancelar</a>
    </div>
  </form>
</div>

<style>
  .pill { display:inline-flex; align-items:center; gap:4px; padding:2px 10px; border-radius:20px; font-weight:600; }
  .pill-success { background:rgba(16,185,129,.1); color:#059669; }
</style>

<script>
  document.getElementById('imageFileInput')?.addEventListener('change', function(e) {
    const file = e.target.files[0];
    const preview = document.getElementById('imgPreview');
    const placeholder = document.getElementById('imgPlaceholder');
    const box = document.getElementById('imgPreviewBox');

    if (file && file.type.startsWith('image/')) {
      const reader = new FileReader();
      reader.onload = function(ev) {
        preview.src = ev.target.result;
        preview.style.display = '';
        if (placeholder) placeholder.style.display = 'none';
        box.style.borderStyle = 'solid';
        box.style.borderColor = '#e2e8f0';
      };
      reader.readAsDataURL(file);
    }
  });
</script>
