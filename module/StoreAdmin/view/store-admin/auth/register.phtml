<?php
// La vista de registro NO usa el layout del admin
$this->layout(false); // No layout
?><!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Regístrate — CITROB</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>
  <style>
    :root{--primary:#E85D22;--primary-dark:#C94D1A;--primary-rgb:232,93,34;}
    body{font-family:'Inter',sans-serif;min-height:100vh;background:linear-gradient(135deg,#1a1a2e,#16213e,#0f3460);display:flex;align-items:center;justify-content:center;overflow:hidden;}
    body::before{content:'';position:fixed;inset:0;background:radial-gradient(ellipse at 20% 50%,rgba(232,93,34,.12),transparent 50%),radial-gradient(ellipse at 80% 20%,rgba(232,93,34,.08),transparent 40%);pointer-events:none;}
    .login-card{background:rgba(255,255,255,.05);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border:1px solid rgba(255,255,255,.1);border-radius:20px;padding:2.5rem;box-shadow:0 25px 50px rgba(0,0,0,.4);width:100%;max-width:420px;position:relative;z-index:10;}
    .form-control{background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.15);color:#fff;border-radius:10px;padding:.7rem 1rem .7rem 2.75rem;font-size:.9rem;}
    .form-control::placeholder{color:rgba(255,255,255,.3);}
    .form-control:focus{background:rgba(255,255,255,.12);border-color:var(--primary);box-shadow:0 0 0 3px rgba(232,93,34,.2);color:#fff;outline:none;}
    .form-label{color:rgba(255,255,255,.75);font-size:.875rem;font-weight:500;}
    .input-wrap{position:relative;}
    .input-icon{position:absolute;left:12px;top:50%;transform:translateY(-50%);color:rgba(255,255,255,.4);font-size:18px;pointer-events:none;}
    .btn-login{background:linear-gradient(135deg,var(--primary),var(--primary-dark));color:#fff;border:none;border-radius:10px;font-weight:700;font-size:.95rem;padding:.8rem;box-shadow:0 6px 20px rgba(232,93,34,.4);transition:all .3s;}
    .btn-login:hover{transform:translateY(-2px);box-shadow:0 10px 28px rgba(232,93,34,.5);color:#fff;}
    .alert-error{background:rgba(220,53,69,.15);border:1px solid rgba(220,53,69,.3);color:#f8a0a8;border-radius:10px;padding:.75rem 1rem;font-size:.875rem;}
    .alert-success{background:rgba(40,167,69,.15);border:1px solid rgba(40,167,69,.3);color:#84e09f;border-radius:10px;padding:.75rem 1rem;font-size:.875rem;}
    .back-link{color:rgba(255,255,255,.4);font-size:.8rem;text-decoration:none;transition:color .2s;}
    .back-link:hover{color:var(--primary);}
    .deco{position:fixed;border-radius:50%;pointer-events:none;animation:float 6s ease-in-out infinite;}
    .deco:nth-child(1){width:300px;height:300px;background:rgba(232,93,34,.05);top:-100px;right:-80px;}
    .deco:nth-child(2){width:200px;height:200px;background:rgba(232,93,34,.04);bottom:50px;left:-60px;animation-delay:2s;}
    @keyframes float{0%,100%{transform:translateY(0) rotate(0)}50%{transform:translateY(-20px) rotate(5deg)}}
  </style>
</head>
<body>
  <div class="deco"></div><div class="deco"></div>
  <div class="login-card">
    <div class="text-center mb-4">
      <img src="/citrob_back/public/imgs/logo.jpeg" alt="CITROB" style="width:64px;height:64px;border-radius:14px;object-fit:cover;box-shadow:0 8px 24px rgba(232,93,34,.4);" onerror="this.style.background='var(--primary)';this.src='data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7';" class="mb-3">
      <h1 style="color:#fff;font-weight:800;font-size:1.6rem;">Únete a <span style="color:var(--primary);">CITROB</span></h1>
      <p style="color:rgba(255,255,255,.5);font-size:.85rem;">Crea una cuenta para guardar tu carrito</p>
    </div>

    <?php if ($this->error): ?>
    <div class="alert-error mb-4 d-flex align-items-center gap-2">
      <span class="material-icons" style="font-size:18px;">error_outline</span>
      <?= htmlspecialchars($this->error, ENT_QUOTES) ?>
    </div>
    <?php endif; ?>
    
    <?php if ($this->success): ?>
    <div class="alert-success mb-4 d-flex align-items-center gap-2">
      <span class="material-icons" style="font-size:18px;">check_circle_outline</span>
      <?= htmlspecialchars($this->success, ENT_QUOTES) ?>
    </div>
    <?php endif; ?>

    <form method="POST" action="<?= $this->url('admin-register') ?>" autocomplete="off">
      <div class="mb-3">
        <label class="form-label">Usuario</label>
        <div class="input-wrap">
          <span class="material-icons input-icon">person</span>
          <input type="text" name="username" class="form-control" placeholder="Elige tu nombre de usuario" required autofocus>
        </div>
      </div>
      <div class="mb-3">
        <label class="form-label">Contraseña</label>
        <div class="input-wrap">
          <span class="material-icons input-icon">lock</span>
          <input type="password" name="password" class="form-control" placeholder="••••••••" required>
        </div>
      </div>
      <div class="mb-4">
        <label class="form-label">Confirmar Contraseña</label>
        <div class="input-wrap">
          <span class="material-icons input-icon">lock_outline</span>
          <input type="password" name="password_confirm" class="form-control" placeholder="••••••••" required>
        </div>
      </div>
      <button type="submit" class="btn btn-login w-100 d-flex align-items-center justify-content-center gap-2">
        <span class="material-icons" style="font-size:18px;">person_add</span>
        Crear Cuenta
      </button>
    </form>

    <div class="text-center mt-3">
      <p style="color:rgba(255,255,255,.5);font-size:.85rem;margin-bottom:0.5rem;">
        ¿Ya tienes cuenta? <a href="<?= $this->url('admin-login') ?>" style="color:var(--primary);text-decoration:none;font-weight:600;">Inicia sesión aquí</a>
      </p>
    </div>

    <hr style="border-color:rgba(255,255,255,.1);margin:1.5rem 0;">
    <div class="text-center">
      <a href="<?= $this->url('shop') ?>" class="back-link d-inline-flex align-items-center gap-1">
        <span class="material-icons" style="font-size:14px;">arrow_back</span>
        Volver al Store
      </a>
    </div>
  </div>
</body>
</html>
