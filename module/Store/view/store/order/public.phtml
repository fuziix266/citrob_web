<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var array $order
 * @var array $items
 */
$this->headTitle('Resumen de Pedido');
?>

<div class="row pt-5">
    <div class="col-12 col-md-8 mx-auto">
        <div class="card shadow-sm border-0 mb-4">
            <div class="card-header bg-dark text-white p-4">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h4 class="mb-0">Resumen de tu Pedido</h4>
                        <small class="text-white-50">Orden #<?= htmlspecialchars((string)$order['id']) ?></small>
                    </div>
                    <?php if ($order['status'] === 'pending'): ?>
                        <span class="badge bg-warning text-dark px-3 py-2">Pendiente de Confirmación</span>
                    <?php elseif ($order['status'] === 'processing'): ?>
                        <span class="badge bg-info px-3 py-2">En Proceso</span>
                    <?php elseif ($order['status'] === 'shipped'): ?>
                        <span class="badge bg-primary px-3 py-2">Enviado</span>
                    <?php elseif ($order['status'] === 'delivered'): ?>
                        <span class="badge bg-success px-3 py-2">Completado</span>
                    <?php elseif ($order['status'] === 'cancelled'): ?>
                        <span class="badge bg-danger px-3 py-2">Cancelado</span>
                    <?php endif; ?>
                </div>
            </div>
            <div class="card-body p-4">
                
                <h5 class="border-bottom pb-2 mb-3">Detalle de Productos</h5>
                
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Producto</th>
                                <th class="text-center">Cantidad</th>
                                <th class="text-end">Precio Unitario</th>
                                <th class="text-end">Subtotal</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($items as $item): ?>
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <?php if (!empty($item['image_url'])): ?>
                                                <img src="<?= htmlspecialchars($item['image_url']) ?>" alt="<?= htmlspecialchars($item['product_name']) ?>" class="img-thumbnail me-3" style="width: 50px; height: 50px; object-fit: cover;">
                                            <?php else: ?>
                                                <div class="bg-light text-center me-3 d-flex align-items-center justify-content-center rounded" style="width: 50px; height: 50px;">
                                                    <span class="material-icons text-muted">inventory_2</span>
                                                </div>
                                            <?php endif; ?>
                                            <div>
                                                <span class="fw-bold d-block"><?= htmlspecialchars($item['product_name']) ?></span>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="text-center"><?= htmlspecialchars((string)$item['qty']) ?></td>
                                    <td class="text-end">$<?= number_format((float)$item['unit_price'], 0, ',', '.') ?></td>
                                    <td class="text-end fw-bold">$<?= number_format((float)$item['subtotal'], 0, ',', '.') ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                        <tfoot class="table-group-divider">
                            <tr>
                                <td colspan="3" class="text-end fw-bold h5 mb-0">Total a Pagar:</td>
                                <td class="text-end fw-bold h5 mb-0 text-primary">$<?= number_format((float)$order['total'], 0, ',', '.') ?></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>

                <div class="alert alert-info mt-4" role="alert">
                    <h5 class="alert-heading d-flex align-items-center gap-2"><span class="material-icons">info</span> Información Importante</h5>
                    <p class="mb-0">Tu orden ha sido registrada en nuestro sistema y está pendiente de revisión. Un administrador validará el stock y te contactará para proceder con el pago y envío.</p>
                </div>

                <div class="text-center mt-4 pt-3 border-top">
                    <a href="<?= $this->url('shop') ?>" class="btn btn-outline-dark px-4 py-2 d-inline-flex align-items-center gap-2">
                        <span class="material-icons" style="font-size:18px;">arrow_back</span> Volver a la Tienda
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
